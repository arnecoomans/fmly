# Python
pip >= 24.3.1

# Django
Django >= 5.1.4
Pillow >= 10.0.0
gunicorn >= 21.2.0

# Markdown
Markdown >= 3.4.4

# Heic to JPG
pillow-heif >= 0.13.1

# Sendfile for Attachments
django-sendfile2 >= 0.7.0

# Graphviz for Family Tree
graphviz >= 0.20.1

# PyMuPDF for PDFs in Image-to-Attachment conversion
PyMuPDF >= 1.24.9